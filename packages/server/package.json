{
  "name": "@city-monitor/server",
  "version": "0.0.1",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:generate": "node --env-file-if-exists=.env ../../node_modules/drizzle-kit/bin.cjs generate",
    "db:migrate": "node --env-file-if-exists=.env ../../node_modules/drizzle-kit/bin.cjs migrate",
    "db:push": "node --env-file-if-exists=.env ../../node_modules/drizzle-kit/bin.cjs push",
    "db:studio": "node --env-file-if-exists=.env ../../node_modules/drizzle-kit/bin.cjs studio",
    "db:invalidate": "tsx --env-file-if-exists=.env src/scripts/invalidate-cache.ts",
    "db:retain": "tsx --env-file-if-exists=.env src/scripts/data-retention.ts"
  },
  "dependencies": {
    "@city-monitor/shared": "*",
    "@langchain/openai": "^1.2.11",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "drizzle-orm": "^0.39",
    "exceljs": "^4.4.0",
    "express": "^5",
    "express-rate-limit": "^8.2.1",
    "fast-xml-parser": "^5.7.0",
    "helmet": "^8.1.0",
    "node-cron": "^3.0.3",
    "postgres": "^3.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/compression": "^1.8.1",
    "@types/cors": "^2.8.17",
    "@types/express": "^5",
    "@types/node-cron": "^3.0.11",
    "drizzle-kit": "^0.30",
    "tsx": "^4",
    "vitest": "^3"
  }
}
